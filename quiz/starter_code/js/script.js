$(document).ready(function(){ 
	console.log("Ready!");

	$("img").click(function() {
		console.log("clicked image");	
	});


	// this function is triggered when the enter button is clicked
	$("#enter_button").click(function() {
		// calculate a score for each category based on chosen images

		// determine winning category from scores

		// print result to HTML
	});

});
